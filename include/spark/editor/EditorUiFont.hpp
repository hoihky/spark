#pragma once

namespace Spark {

class GameWorld;

/** Loads Roboto UI fonts into the world (required for editor panels and labels). */
void MountEditorUiFonts(GameWorld& world);

}  // namespace Spark
